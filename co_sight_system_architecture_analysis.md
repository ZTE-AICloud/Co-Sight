### 1. 系统架构总结

**整体系统架构:**

Co-Sight 是一个先进的、基于代理（Agent）的系统，专为研究和报告生成而设计，主要利用大型语言模型（LLM）。其架构特点如下：

*   **客户端-服务器架构:** 基于 Web 的前端（客户端）与 Python 后端服务器交互。
*   **基于代理（Agent-Based）:**核心逻辑围绕自主代理（规划器 Planner Agent 和执行器 Actor Agent）展开，它们协同工作以分解任务、执行步骤并综合信息。
*   **事件驱动（用于 UI 更新）:** 后端采用事件机制，将研究计划的实时进度更新流式传输到前端。
*   **模块化和面向工具:** 系统由代理、工具、任务管理和 LLM 交互等不同模块构建而成。代理利用丰富的工具集执行诸如 Web 搜索、文件操作和数据分析等操作。

**关键组件:**

1.  **前端 (`cosight_ui` - Angular):**
    *   提供用户界面，供用户输入研究查询/任务。
    *   动态显示研究计划的进度，包括步骤、状态、备注和生成的文件。
    *   呈现最终生成的报告。
    *   通过 HTTP API 调用与后端通信，并可能通过 WebSocket 进行实时更新（尽管观察到的主要流式传输机制是 HTTP 流）。

2.  **后端 (`cosight_server` - FastAPI):**
    *   使用 FastAPI 框架构建的 Python 服务器。
    *   **API 端点:** 公开 RESTful API 用于前端通信。最关键的端点是 `/deep-research/search`，它启动任务并流式传输结果。其他端点处理用户管理、WebSocket 连接（如果更广泛使用）和通用实用程序。
    *   **请求处理:** 管理传入请求，验证输入，并编排任务执行过程。
    *   **静态文件服务:** 提供 Angular 前端应用程序和工作区中生成的文件。
    *   **实时更新:** 实现 HTTP 流式传输，将计划进度发送到客户端。

3.  **核心逻辑 (`app` 目录 - Python):**
    *   **`CoSight` 编排器 (`CoSight.py`):** 管理研究任务整个生命周期的中心类。它协调规划阶段和代理执行各个计划步骤。
    *   **LLM 代理 (`app/cosight/agent/`):**
        *   **`TaskPlannerAgent`:** 由 LLM 驱动的代理，负责理解用户查询、创建多步骤研究计划（任务的有向无环图）、必要时重新规划，并生成最终摘要/报告。
        *   **`TaskActorAgent`:** 由 LLM 驱动的代理（多个实例可以并发运行），负责执行计划的各个步骤。每个执行器代理使用各种工具来执行其分配的子任务。
        *   **`BaseAgent`:** 为代理与 LLM 的交互提供核心机制的基础类，包括管理对话历史和编排工具调用（函数调用）。
    *   **任务管理 (`app/cosight/task/`):**
        *   **`Plan` 对象 (`todolist.py`):** 表示研究任务状态的关键数据结构。它存储计划的标题、步骤、步骤之间的依赖关系、步骤状态（例如，not_started、in_progress、completed、blocked）、备注以及指向生成文件的链接。它还管理工作区内的文件发现。
        *   **`PlanReportManager`:** 一个事件管理器，允许系统的不同部分发布和订阅与计划更新相关的事件（例如，计划已创建、步骤已处理、最终结果已准备就绪）。这是向 UI 流式传输更新的关键。
    *   **工具集 (`app/cosight/tool/`):** Python 模块和函数的集合，为 `TaskActorAgent` 提供特定功能。示例包括 Web 搜索（Google、Tavily、Baidu、Wiki）、文件操作（读、写、修改）、代码执行、网页抓取、文档内容提取、图像和视频分析以及 HTML 报告生成。
    *   **LLM 接口 (`app/cosight/llm/` 和根目录 `llm.py`):** 负责配置和与各种 LLM（用于规划、执行、工具使用、视觉）交互的模块。

4.  **工作区 (`work_space/` 目录):**
    *   为每个研究任务创建的专用目录。
    *   存储任务执行期间所有相关的中间文件、日志、下载内容、代码执行输出和最终报告。
    *   `Plan` 对象和工具在此工作区内主动管理和引用文件。

5.  **配置 (`config/` 和 `.env`):**
    *   管理 LLM API 密钥、模型名称、搜索引擎密钥、服务器端口和其他操作参数的设置。

该架构使 Co-Sight 能够通过将复杂研究任务分解为可管理的步骤，利用 LLM 进行推理和行动，并使用多样化的工具集与数字环境交互和处理信息。
